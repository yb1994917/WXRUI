<rui-list-group rui-class="rui-list-newcontent">
  <rui-panel title="单行列表和描述"></rui-panel>
  <rui-list rui-class="{{index !== homeList.length - 1 ? 'rui-list-newli' : ''}}" wx:key="homeList" wx:for="{{homeList}}" title="{{item.title}}" url="{{item.url}}"></rui-list>
</rui-list-group>
